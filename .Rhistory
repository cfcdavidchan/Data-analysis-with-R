data(mtcars)
names(mtcars)
View(mtcars)
View(mtcars)
data('mtcars')
View(mtcars)
summary(mtcars)
efficient <- subset(mtcars,mpg >= 21)
dim(efficient)[1]
subset(mtcars, mpg > 30 & hp > 100)
subset(mtcars, mpg < 14 | disp > 390)
subset(mtcars,qsec<=16.9)
lightcars <- subset(mtcars,wt<2)
dim(lightcars)[1]
mtcars$year <- 1974
mtcars <- subset(mtcars, select = -year)
mtcars$year <- c(1973, 1974)
mtcars <- subset(mtcars, select = -year)
mtcars$wt
cond <- mtcars$wt < 3
cond
mtcars$weight_class <- ifelse(cond, 'light', 'average')
mtcars$weight_class
cond <- mtcars$wt > 3.5
mtcars$weight_class <- ifelse(cond, 'heavy', mtcars$weight_class)
mtcars$weight_class
rm(cond)
rm(efficient)
install.packages('knitr', dependencies = T)
library(knitr)
subset(mtcar,mpg>=30|hp<60)
subset(mtcars,mpg>=30|hp<60)
View(mtcars)
View(mtcars)
mtcars$year <- 1974
mtcars <- subset(mtcars, select = -year)
